describe("Document preview test (Manual)", () => {
    it("Parse documents and display in HTML", () => {
        expect(true).toBeTruthy();
    })

    it("Load documents from cloud storage for display", () => {
        expect(true).toBeTruthy();
    })

})