describe("Meeting transcription test (Manual)", () => {
    it("Upload video for transcription", () => {
        expect(true).toBeTruthy();
    })

    it("Handling errors due to inappropriate file type", () => {
        expect(false).toBeFalsy();
    })

    it("Handling errors in request", () => {
        expect(false).toBeFalsy();
    })
})